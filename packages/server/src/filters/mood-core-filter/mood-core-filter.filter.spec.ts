import { MoodCoreFilterFilter } from './mood-core-filter.filter';

describe('MoodCoreFilterFilter', () => {
  it('should be defined', () => {
    expect(new MoodCoreFilterFilter()).toBeDefined();
  });
});
